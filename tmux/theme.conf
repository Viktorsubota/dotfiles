# Theme styles (sourced after color definitions)

# Status bar
set -gF status-style "bg=#{@thm_mantle},fg=#{@thm_fg}"
set -g status-left ""
set -g status-right-length 200

# Panes — borders match bg (invisible), inactive panes dimmed
set -gF pane-border-style "fg=#{@thm_surface_0},bg=#{@thm_mantle}"
set -gF pane-active-border-style "fg=#{@thm_surface_0},bg=#{@thm_mantle}"
set -gF window-style "bg=#{@thm_mantle}"
set -gF window-active-style "bg=#{@thm_bg}"

# Messages
set -gF message-style "fg=#{@thm_teal},bg=#{@thm_overlay_0},align=centre"
set -gF message-command-style "fg=#{@thm_teal},bg=#{@thm_overlay_0},align=centre"

# Copy mode and clock
set -gF mode-style "bg=#{@thm_surface_0},bold"
set -gF clock-mode-colour "#{@thm_blue}"

# Windows
set -g window-status-separator " "
set -gF window-status-format " #[fg=#{@thm_crust},bg=#{@thm_overlay_2}] ##I #[fg=#{@thm_fg},bg=#{@thm_surface_0}] ##W "
set -gF window-status-current-format " #[fg=#{@thm_crust},bg=#{@thm_sapphire}] ##I #[fg=#{@thm_fg},bg=#{@thm_surface_1}] ##W "
set -gF window-status-activity-style "bg=#{@thm_lavender},fg=#{@thm_crust}"
set -gF window-status-bell-style "bg=#{@thm_yellow},fg=#{@thm_crust}"

# Status right modules
set -gF @status_directory "#[fg=#{@thm_rosewater}]#[fg=#{@thm_crust},bg=#{@thm_rosewater}] #[fg=#{@thm_fg},bg=#{@thm_surface_0}] ##{b:pane_current_path}#[fg=#{@thm_surface_0}] "
set -gF @status_session "#[fg=##{?client_prefix,#{@thm_red},#{@thm_green}}]#[fg=#{@thm_crust},bg=##{?client_prefix,#{@thm_red},#{@thm_green}}] #[fg=#{@thm_fg},bg=#{@thm_surface_0}] ##S#[fg=#{@thm_surface_0}] "
set -gF @status_zoom "#[fg=#{@thm_yellow}]#[fg=#{@thm_crust},bg=#{@thm_yellow}] #[fg=#{@thm_yellow},bg=#{@thm_mantle}]"

set -g status-right "#{E:@status_directory}"
set -ag status-right "#{E:@status_session}"
set -ag status-right "#{?window_zoomed_flag,#{E:@status_zoom},}"
